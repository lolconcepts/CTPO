<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
       <a class="navbar-brand" href="#">Church 2.0&nbsp;</a>
       <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
       <span class="navbar-toggler-icon"></span>
       </button>
       <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
             <li class="nav-item active">
                <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
             </li>
             <% if current_user && current_user.admin %>
             <li class="nav-item">
                <a class="nav-link" href="#">Parishoners</a>
             </li>
             <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Administration
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                   <a class="dropdown-item" href="#">Show Parishoners</a>
                   <a class="dropdown-item" href="#">Print Name Tags</a>
                   <div class="dropdown-divider"></div>
                   <a class="dropdown-item" href="#">Edit Church Info</a>
					<a class="dropdown-item" href="#">Print Offering Cards</a>
					<a class="dropdown-item" href="#">Notification Center</a>
                </div>
             </li>
             <%end%>
              <li class="nav-item">
                <a class="nav-link" href="/nametag" target="_blank">Print Name Tag</a>
             </li>
              <li class="nav-item">
                <a class="nav-link" href="#" target="_blank">Make An Offering</a>
             </li>
             <li class="nav-item text-right">
             	<% if current_user %>
             		<%= link_to "Logged in as #{current_user.fullname}", destroy_user_session_path, method: :delete, :class => "nav-link" %>
             		
             	<% end %>
             </li>
          </ul>
       </div>
    </nav>

    <div class="jumbotron jumbotron-fluid text-center">
       <h1 class="display-4">
        <% if @church %>
        <%= @church.name %>
        <% end %>

      &nbsp;</h1>
       <p class="lead">Welcome&nbsp;</p>
       <hr class="my-4">
       <p>If you have any issus with CTPO, contact support @ <a href="mailto:info@lolconcepts.com">info@lolconcepts.com</a>&nbsp;&nbsp;</p>
       <p class="lead">
          
       </p>
                    	<% if current_user %>
					<%= link_to 'Sign Out', destroy_user_session_path, method: :delete, :class => "nav-link" %>
					<%= link_to 'Edit', edit_user_registration_url, :class => "nav-link" %>
				<% else %>
					<%= link_to 'Sign up', new_user_registration_path, :class => "nav-link" %>
					<%= link_to 'Sign In', new_user_session_path, :class => "nav-link" %>
				<% end %>
    </div>


